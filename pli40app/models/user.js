class User {
    constructor(id, email, password, name, surname, photoRoute, tripList, searchList ){
        this.id = id;
        this.email = email;
        this. password = password;
        this.name = name;
        this.surname = surname;
        this.photoRoute = photoRoute;
    }
}

export default User;